# YOLO development patch (load plugin from source, no build needed).
# Use with: pnpm dsh web --patch D:/Code/WorkBuddy/dsh-yolo/cordis.dev.yml
# `name` is an absolute path to the plugin entry module; dsh loader resolves .ts directly.
- insert:
  - id: yolo
    name: 'D:/Code/WorkBuddy/dsh-yolo/src/index.ts'
